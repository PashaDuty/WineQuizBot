# 📖 ПОЛНАЯ ИНСТРУКЦИЯ: Как перенести бота на хостинг (для новичков)

## 🎯 Что мы будем делать?

Мы перенесём вашего бота с вашего компьютера на специальный сервер в интернете, чтобы он работал 24 часа в сутки, даже когда ваш компьютер выключен.

---

## 📋 ЧТО НУЖНО ПОДГОТОВИТЬ ПЕРЕД НАЧАЛОМ

### 1. Выбрать хостинг (место для бота)

**Рекомендую: Timeweb Cloud** (самый простой для новичков)

**Как зарегистрироваться:**
1. Откройте браузер (Chrome, Firefox, Edge)
2. Перейдите на сайт: https://timeweb.com/cloud/
3. Нажмите кнопку "Регистрация" или "Войти"
4. Заполните форму (email, пароль)
5. Подтвердите email (проверьте почту)

**Как создать сервер:**
1. После входа нажмите "Создать сервер" или "VPS"
2. Выберите:
   - **ОС**: Ubuntu 22.04 (или последняя версия)
   - **Тариф**: Самый дешёвый (1 CPU, 1GB RAM, 20GB) - этого достаточно!
   - **Регион**: Выберите ближайший к России
3. Нажмите "Создать" или "Оплатить"
4. **ВАЖНО!** Сохраните:
   - IP-адрес сервера (например: 123.45.67.89)
   - Логин (обычно `root`)
   - Пароль (или ключ SSH)

### 2. Что нужно знать о вашем компьютере

- У вас Windows? Отлично, инструкция для Windows!
- Нужна программа для подключения к серверу (скачаем ниже)

---

## 🛠️ ШАГ 1: Установка программы для подключения к серверу

### Что такое SSH?

SSH — это способ подключиться к серверу через интернет, как будто вы сидите за ним.

### Установка PuTTY (для Windows)

1. Откройте браузер
2. Перейдите: https://www.putty.org/
3. Нажмите "Download" или "Скачать"
4. Скачайте файл `putty.exe` (или `putty-64bit.exe`)
5. Сохраните на Рабочий стол
6. **Готово!** Программа установлена (не нужно ничего устанавливать, просто запускайте файл)

**Альтернатива (если у вас Windows 10/11):**
- Можно использовать встроенную программу "Терминал" (но PuTTY проще)

---

## 🔌 ШАГ 2: Подключение к серверу

### 2.1. Открыть PuTTY

1. Найдите файл `putty.exe` на Рабочем столе
2. Дважды кликните на него
3. Откроется окно PuTTY

### 2.2. Ввести данные сервера

В окне PuTTY:

1. **Host Name (или IP address)**: Вставьте IP-адрес вашего сервера
   - Например: `123.45.67.89`
   - Или: `root@123.45.67.89`

2. **Port**: Оставьте `22` (это стандартный порт)

3. **Connection type**: Выберите `SSH` (должно быть выбрано по умолчанию)

4. Нажмите кнопку **"Open"** (Открыть) внизу

### 2.3. Первое подключение

1. Появится окно с предупреждением "Security Alert"
2. Нажмите **"Да"** или **"Yes"** (это нормально при первом подключении)

3. Откроется чёрное окно (это терминал — командная строка сервера)

4. Введите логин (обычно `root`) и нажмите Enter

5. Введите пароль (когда будете вводить, символы НЕ будут видны — это нормально!)
   - Просто введите пароль и нажмите Enter

6. Если всё правильно, увидите строку типа:
   ```
   root@server:~#
   ```
   Это значит, вы подключились! 🎉

---

## 📦 ШАГ 3: Подготовка файлов на вашем компьютере

### 3.1. Проверить, что у вас есть

Откройте папку `WineQuizBot` на рабочем столе и проверьте:

- ✅ Есть файл `.env` (с токеном бота)
- ✅ Есть все папки: `data`, `handlers`, и т.д.
- ✅ Есть файл `bot.py`

### 3.2. Создать архив проекта (опционально)

**Если будете загружать через SCP (без Git):**

1. Откройте папку `WineQuizBot`
2. Выделите ВСЕ файлы (Ctrl+A)
3. Правой кнопкой → "Отправить" → "Сжатая ZIP-папка"
4. Назовите архив `WineQuizBot.zip`

**ВАЖНО:** Не включайте в архив файл `.env` и `database.db` (если они есть)!

---

## 📁 ШАГ 4: Загрузка проекта на сервер

### Вариант А: Через WinSCP (самый простой для новичков)

#### 4.1. Скачать WinSCP

1. Откройте: https://winscp.net/eng/download.php
2. Скачайте "WinSCP" (установщик)
3. Установите программу (просто нажимайте "Далее")

#### 4.2. Подключиться к серверу через WinSCP

1. Откройте WinSCP
2. В поле **"Имя хоста"**: вставьте IP-адрес сервера
3. **"Имя пользователя"**: `root`
4. **"Пароль"**: ваш пароль от сервера
5. Нажмите **"Войти"**

6. Откроется окно с двумя панелями:
   - **Слева**: ваш компьютер
   - **Справа**: сервер

#### 4.3. Загрузить файлы

1. **Слева** найдите папку `WineQuizBot` на вашем компьютере
2. **Справа** перейдите в папку `/root` (или создайте папку `WineQuizBot`)
3. Перетащите ВСЕ файлы из левой панели в правую
   - Или выделите файлы и нажмите кнопку "→" (загрузить)

4. Дождитесь окончания загрузки

### Вариант Б: Через Git (если проект в GitHub)

**Если вы используете Git (GitHub):**

1. В PuTTY (чёрное окно) введите:
   ```bash
   cd ~
   ```
   (это перейти в домашнюю папку)

2. Введите:
   ```bash
   git clone https://github.com/ваш_username/WineQuizBot.git
   ```
   (замените на ваш адрес репозитория)

3. Нажмите Enter

4. Дождитесь загрузки

---

## ⚙️ ШАГ 5: Установка Python и зависимостей на сервере

### 5.1. Обновить систему

В PuTTY (чёрное окно) введите команды по очереди (после каждой нажимайте Enter):

```bash
sudo apt update
```

Подождите, пока выполнится (может занять 1-2 минуты).

```bash
sudo apt upgrade -y
```

Подождите (может занять 5-10 минут).

### 5.2. Установить Python и необходимые программы

Введите:

```bash
sudo apt install -y python3 python3-pip python3-venv git
```

Подождите установки.

### 5.3. Перейти в папку проекта

```bash
cd WineQuizBot
```

(или `cd ~/WineQuizBot` если проект в домашней папке)

### 5.4. Создать виртуальное окружение

```bash
python3 -m venv venv
```

Подождите 10-20 секунд.

### 5.5. Активировать виртуальное окружение

```bash
source venv/bin/activate
```

После этой команды в начале строки появится `(venv)` — это значит, что всё правильно!

### 5.6. Обновить pip

```bash
pip install --upgrade pip
```

Подождите.

### 5.7. Установить зависимости бота

```bash
pip install -r requirements.txt
```

**Это займёт 2-5 минут!** Подождите, пока установятся все библиотеки.

---

## 🔐 ШАГ 6: Создать файл .env на сервере

### 6.1. Открыть редактор

В PuTTY введите:

```bash
nano .env
```

Откроется редактор nano (простой текстовый редактор).

### 6.2. Вставить настройки

Скопируйте с вашего компьютера содержимое файла `.env` и вставьте в nano.

Должно быть примерно так:

```
BOT_TOKEN=8391460994:AAHLH99bzXEywMa4cKfUi33Vk3Y-zx-pgDs
ADMIN_ID=187200098
TIME_PER_QUESTION=30
MIN_QUESTIONS=10
```

### 6.3. Сохранить файл

1. Нажмите **Ctrl + O** (сохранить)
2. Нажмите **Enter** (подтвердить имя файла)
3. Нажмите **Ctrl + X** (выйти из редактора)

---

## 🔄 ШАГ 7: Настроить автозапуск бота

### 7.1. Создать файл сервиса

В PuTTY введите:

```bash
sudo nano /etc/systemd/system/winequizbot.service
```

### 7.2. Вставить содержимое

Скопируйте и вставьте в nano (замените `/root` на ваш путь, если проект не в `/root/WineQuizBot`):

```
[Unit]
Description=Wine Quiz Telegram Bot
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/WineQuizBot
Environment="PATH=/root/WineQuizBot/venv/bin"
ExecStart=/root/WineQuizBot/venv/bin/python /root/WineQuizBot/bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

**ВАЖНО:** Замените `/root/WineQuizBot` на реальный путь к вашему проекту!

### 7.3. Сохранить

1. **Ctrl + O**
2. **Enter**
3. **Ctrl + X**

### 7.4. Активировать сервис

Введите команды по очереди:

```bash
sudo systemctl daemon-reload
```

```bash
sudo systemctl enable winequizbot
```

```bash
sudo systemctl start winequizbot
```

### 7.5. Проверить, что бот запустился

```bash
sudo systemctl status winequizbot
```

**Что должно быть:**
- Зелёная надпись "active (running)"
- Нет красных ошибок

**Если есть ошибки:**
- Нажмите `q` чтобы выйти
- Посмотрите логи (см. ниже)

---

## 📊 ШАГ 8: Проверка работы бота

### 8.1. Посмотреть логи

В PuTTY введите:

```bash
sudo journalctl -u winequizbot -n 50
```

**Что искать:**
- ✅ `[BOT] Bot zapushen: @quizwinebot` — бот запущен!
- ✅ `Start polling` — бот работает!
- ❌ Если видите ошибки — скопируйте их и покажите мне

### 8.2. Проверить в Telegram

1. Откройте Telegram
2. Найдите вашего бота: `@quizwinebot`
3. Напишите `/start`
4. Если бот отвечает — **ВСЁ РАБОТАЕТ!** 🎉

---

## 🔧 ПОЛЕЗНЫЕ КОМАНДЫ (сохраните их!)

### Остановить бота
```bash
sudo systemctl stop winequizbot
```

### Запустить бота
```bash
sudo systemctl start winequizbot
```

### Перезапустить бота
```bash
sudo systemctl restart winequizbot
```

### Посмотреть логи в реальном времени
```bash
sudo journalctl -u winequizbot -f
```
(Чтобы выйти: нажмите `Ctrl + C`)

### Посмотреть последние 100 строк логов
```bash
sudo journalctl -u winequizbot -n 100
```

---

## 🆘 РЕШЕНИЕ ПРОБЛЕМ

### Проблема 1: "Permission denied" (доступ запрещён)

**Решение:**
```bash
sudo chmod -R 755 /root/WineQuizBot
```

### Проблема 2: Бот не запускается

**Что делать:**
1. Посмотрите логи: `sudo journalctl -u winequizbot -n 100`
2. Проверьте, что файл `.env` существует: `ls -la .env`
3. Проверьте, что токен правильный: `cat .env`

### Проблема 3: "Module not found" (модуль не найден)

**Решение:**
```bash
cd /root/WineQuizBot
source venv/bin/activate
pip install -r requirements.txt
sudo systemctl restart winequizbot
```

### Проблема 4: Не могу подключиться к серверу

**Проверьте:**
- Правильный ли IP-адрес?
- Правильный ли пароль?
- Работает ли интернет?

---

## 📝 ЧЕК-ЛИСТ: Всё ли готово?

- [ ] Зарегистрирован на хостинге
- [ ] Создан сервер
- [ ] Установлен PuTTY
- [ ] Подключился к серверу через PuTTY
- [ ] Загрузил файлы проекта на сервер
- [ ] Установил Python и зависимости
- [ ] Создал файл `.env` на сервере
- [ ] Настроил автозапуск (systemd)
- [ ] Бот запущен и работает
- [ ] Проверил в Telegram — бот отвечает

---

## 💡 СОВЕТЫ ДЛЯ НОВИЧКОВ

1. **Не бойтесь ошибиться** — всё можно исправить!
2. **Копируйте команды точно** — даже лишний пробел может вызвать ошибку
3. **Сохраняйте пароли** в безопасном месте
4. **Делайте бэкапы** — периодически скачивайте файл `database.db`

---

## 🎉 ГОТОВО!

Если вы дошли до этого места и бот работает — **вы молодец!** 🎊

Теперь ваш бот работает 24/7 на сервере, и пользователи могут играть в викторину в любое время!

---

## ❓ НУЖНА ПОМОЩЬ?

Если что-то не получается:
1. Скопируйте текст ошибки
2. Сделайте скриншот
3. Напишите мне — помогу разобраться!
