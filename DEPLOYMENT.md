# 🚀 План переноса бота на хостинг

## 📋 Чек-лист перед переносом

- [ ] Выбран хостинг
- [ ] Создан аккаунт на хостинге
- [ ] Создан VPS/сервер
- [ ] Получен доступ по SSH

---

## 📦 ШАГ 1: Подготовка на локальной машине

### 1.1. Проверка файлов

Убедитесь, что у вас есть:
- ✅ Все файлы проекта
- ✅ Файл `.env` с токеном (НЕ загружайте его в Git!)
- ✅ База данных `database.db` (если нужна история)

### 1.2. Создание архива проекта

```bash
# В папке проекта создайте архив (БЕЗ .env и database.db)
tar -czf winequizbot.tar.gz \
  --exclude='.env' \
  --exclude='database.db' \
  --exclude='__pycache__' \
  --exclude='*.pyc' \
  --exclude='.git' \
  .
```

Или используйте `.gitignore` и загрузите через Git.

---

## 🖥️ ШАГ 2: Настройка сервера

### 2.1. Подключение к серверу

```bash
ssh root@ваш_сервер_ip
# или
ssh username@ваш_сервер_ip
```

### 2.2. Обновление системы

```bash
# Для Ubuntu/Debian:
sudo apt update
sudo apt upgrade -y

# Установка необходимых пакетов:
sudo apt install -y python3 python3-pip python3-venv git
```

### 2.3. Создание пользователя для бота (рекомендуется)

```bash
# Создать пользователя
sudo adduser winebot
sudo usermod -aG sudo winebot

# Переключиться на пользователя
su - winebot
```

---

## 📁 ШАГ 3: Загрузка проекта

### Вариант А: Через Git (рекомендуется)

```bash
cd ~
git clone https://github.com/ваш_username/WineQuizBot.git
cd WineQuizBot
```

### Вариант Б: Через SCP (если нет Git)

```bash
# На локальной машине:
scp winequizbot.tar.gz root@ваш_сервер_ip:/root/

# На сервере:
cd ~
tar -xzf winequizbot.tar.gz
cd WineQuizBot
```

---

## ⚙️ ШАГ 4: Установка зависимостей

```bash
# Создать виртуальное окружение
python3 -m venv venv

# Активировать окружение
source venv/bin/activate

# Установить зависимости
pip install --upgrade pip
pip install -r requirements.txt
```

---

## 🔐 ШАГ 5: Настройка конфигурации

```bash
# Создать файл .env
nano .env
```

Вставьте:
```env
BOT_TOKEN=ваш_токен_от_BotFather
ADMIN_ID=ваш_telegram_id
TIME_PER_QUESTION=30
MIN_QUESTIONS=10
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

---

## 🔄 ШАГ 6: Настройка автозапуска (systemd)

### 6.1. Создать сервисный файл

```bash
sudo nano /etc/systemd/system/winequizbot.service
```

Вставьте (замените `/home/winebot` на ваш путь):

```ini
[Unit]
Description=Wine Quiz Telegram Bot
After=network.target

[Service]
Type=simple
User=winebot
WorkingDirectory=/home/winebot/WineQuizBot
Environment="PATH=/home/winebot/WineQuizBot/venv/bin"
ExecStart=/home/winebot/WineQuizBot/venv/bin/python /home/winebot/WineQuizBot/bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

### 6.2. Активировать сервис

```bash
# Перезагрузить systemd
sudo systemctl daemon-reload

# Включить автозапуск
sudo systemctl enable winequizbot

# Запустить бота
sudo systemctl start winequizbot

# Проверить статус
sudo systemctl status winequizbot
```

### 6.3. Просмотр логов

```bash
# Посмотреть логи
sudo journalctl -u winequizbot -f

# Посмотреть последние 100 строк
sudo journalctl -u winequizbot -n 100
```

---

## 💾 ШАГ 7: Настройка бэкапов

### 7.1. Создать скрипт бэкапа

```bash
nano ~/backup_bot.sh
```

Вставьте:
```bash
#!/bin/bash
BACKUP_DIR="/home/winebot/backups"
DATE=$(date +%Y%m%d_%H%M%S)
mkdir -p $BACKUP_DIR

# Бэкап базы данных
cp /home/winebot/WineQuizBot/database.db $BACKUP_DIR/database_$DATE.db

# Бэкап конфига
cp /home/winebot/WineQuizBot/.env $BACKUP_DIR/env_$DATE.env

# Удалить старые бэкапы (старше 7 дней)
find $BACKUP_DIR -name "*.db" -mtime +7 -delete
find $BACKUP_DIR -name "*.env" -mtime +7 -delete

echo "Backup completed: $DATE"
```

Сделать исполняемым:
```bash
chmod +x ~/backup_bot.sh
```

### 7.2. Настроить cron для автоматических бэкапов

```bash
crontab -e
```

Добавьте (бэкап каждый день в 3:00):
```
0 3 * * * /home/winebot/backup_bot.sh >> /home/winebot/backup.log 2>&1
```

---

## 🔒 ШАГ 8: Безопасность

### 8.1. Настроить firewall (если нужно)

```bash
# Установить ufw
sudo apt install ufw

# Разрешить SSH
sudo ufw allow 22/tcp

# Включить firewall
sudo ufw enable
```

### 8.2. Обновлять систему регулярно

```bash
# Добавить в crontab
0 2 * * 0 sudo apt update && sudo apt upgrade -y
```

---

## ✅ ШАГ 9: Тестирование

1. Проверьте, что бот запущен:
   ```bash
   sudo systemctl status winequizbot
   ```

2. Проверьте логи:
   ```bash
   sudo journalctl -u winequizbot -n 50
   ```

3. Напишите боту в Telegram `/start`

4. Проверьте админ-панель: `/admin`

---

## 🔧 Полезные команды

```bash
# Остановить бота
sudo systemctl stop winequizbot

# Запустить бота
sudo systemctl start winequizbot

# Перезапустить бота
sudo systemctl restart winequizbot

# Посмотреть логи в реальном времени
sudo journalctl -u winequizbot -f

# Обновить код (если используете Git)
cd ~/WineQuizBot
git pull
source venv/bin/activate
pip install -r requirements.txt
sudo systemctl restart winequizbot
```

---

## 📊 Мониторинг ресурсов

```bash
# Проверить использование CPU и памяти
htop
# или
top

# Проверить место на диске
df -h

# Проверить использование памяти
free -h
```

---

## 🆘 Решение проблем

### Бот не запускается

1. Проверьте логи: `sudo journalctl -u winequizbot -n 100`
2. Проверьте `.env` файл
3. Проверьте права доступа к файлам
4. Убедитесь, что виртуальное окружение активировано в systemd

### Бот падает

1. Проверьте логи на ошибки
2. Убедитесь, что все зависимости установлены
3. Проверьте, что база данных доступна для записи

### Нет доступа к боту

1. Проверьте, что токен правильный
2. Проверьте интернет-соединение сервера
3. Проверьте, что бот запущен: `sudo systemctl status winequizbot`

---

## 💰 Примерная стоимость

- **Timeweb Cloud**: ~200-300₽/мес
- **Selectel**: ~250-350₽/мес
- **Yandex Cloud**: ~300-500₽/мес

Для вашего бота (мало пользователей) достаточно минимального тарифа!

---

## 📝 Чек-лист после переноса

- [ ] Бот запущен и работает
- [ ] Автозапуск настроен
- [ ] Бэкапы настроены
- [ ] Логи проверены
- [ ] Тестирование пройдено
- [ ] Мониторинг настроен

---

**Готово! Ваш бот теперь работает на сервере 24/7! 🎉**
